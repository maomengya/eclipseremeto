package 第二章_顺序表;

public class L_2_3 {                        
//	Integer[] values = {70,20,80,30,60};                   //java自动将int包装成Integer实例
//	SeqList<Integer>list1 = new SeqList<Integer>(values);             //顺序表对象
//	SortedSeqList<Integer>slist1 = new SortedSeqList<Integer>(list1);         //子类，由顺序表构造排序顺序表
//	list1.insert(0,10);                                              //调用insert(i,x)方法，顺序表插入
//	list1.insert(50);                                                 //父类对象调用父类方法，顺序表尾插入
//	slist1.insert(50);                                          //子类对象调用子类方法，排序顺序表按值插入，覆盖
//	System.out.println("list1="+list1.toString());
//	System.out.println("slist1="+slist1.toString());                   //子类继承toString()
//	slist.insert(0,90);                                          //排序顺序表插入，抛出异常
}
